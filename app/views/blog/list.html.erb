<div class="m-5">
    <h1>Le blog</h1>
    
    <div class="row mb-2">

    <% @articles.each do |article| %>
        <div class="col-md-6">
        <div class="row no-gutters border rounded overflow-hidden flex-md-row mb-4 shadow-sm h-md-250 position-relative">
        <div class="col p-4 d-flex flex-column position-static">
            <strong class="d-inline-block mb-2 text-primary"><%= article['catégorie'] %></strong>
            <h3 class="mb-0"><%= article['titre'] %></h3>
            <div class="mb-1 text-muted"><%= article['date'] %></div>
            <p class="card-text mb-auto"><%=  article['intro']%></p>
            <a class="stretched-link" href="<%= article['url'] %>">Lire la suite</a>
        </div>
        <div class="col-auto d-none d-lg-block">
            <img height="200px" src="<%= image_path(article['image']) %>" />
        </div>
        </div>
        </div>
    <% end %>
    </div>
</div>
